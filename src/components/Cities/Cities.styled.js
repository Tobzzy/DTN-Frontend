import styled from "styled-components";

export const Cities = styled.ul(() => ({
  alignItems: "center",
  display: "flex",
  flexDirection: "column",
  margin: 0,
  maxWidth: "300px",
  padding: 0,
}));
